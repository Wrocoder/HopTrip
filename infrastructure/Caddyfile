{
    email {$ACME_EMAIL}
}

{$HOPTRIP_DOMAIN} {
    encode gzip

    @api path /api/* /health /health/* /go/*
    handle @api {
        reverse_proxy api:8000
    }

    handle {
        reverse_proxy web:3000
    }

}
